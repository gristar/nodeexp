extends layout

block content
    form(method='post',action='insert/add')
        span username:
        input(name='username')
        span password:
        input(name='password',type='password')
        button(type='submit') 提交
